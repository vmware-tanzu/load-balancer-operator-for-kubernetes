apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: akodeploymentconfigs.network.tanzu.vmware.com
spec:
  group: network.tanzu.vmware.com
  names:
    kind: AKODeploymentConfig
    listKind: AKODeploymentConfigList
    plural: akodeploymentconfigs
    singular: akodeploymentconfig
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: AKODeploymentConfig is the Schema for the akodeploymentconfigs
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: AKODeploymentConfigSpec defines the desired state of an AKODeploymentConfig
            AKODeploymentConfig describes the shared configurations for AKO deployments
            across a set of Clusters.
          properties:
            adminCredentialRef:
              description: "AdminCredentialRef points to a Secret resource which includes
                the username and password to access and configure the Avi Controller.
                \n * username                   Username used with basic authentication
                for                              the Avi REST API * password                   Password
                used with basic authentication for                              the
                Avi REST API \n This credential needs to be bound with admin tenant
                and will be used by AKO Operator to automate configurations and operations."
              properties:
                name:
                  description: Name is the name of resource being referenced.
                  type: string
                namespace:
                  description: Namespace of the resource being referenced.
                  type: string
              required:
              - name
              - namespace
              type: object
            certificateAuthorityRef:
              description: "CertificateAuthorityRef points to a Secret resource that
                includes the AVI Controller's CA \n * certificateAuthorityData   PEM-encoded
                certificate authority                              certificates"
              properties:
                name:
                  description: Name is the name of resource being referenced.
                  type: string
                namespace:
                  description: Namespace of the resource being referenced.
                  type: string
              required:
              - name
              - namespace
              type: object
            cloudName:
              description: CloudName speficies the AVI Cloud AKO will be deployed
                with
              type: string
            clusterSelector:
              description: Label selector for Clusters. The Clusters that are selected
                by this will be the ones affected by this AKODeploymentConfig. It
                must match the Cluster labels. This field is immutable. By default
                AviClusterLabel is used
              properties:
                matchExpressions:
                  description: matchExpressions is a list of label selector requirements.
                    The requirements are ANDed.
                  items:
                    description: A label selector requirement is a selector that contains
                      values, a key, and an operator that relates the key and values.
                    properties:
                      key:
                        description: key is the label key that the selector applies
                          to.
                        type: string
                      operator:
                        description: operator represents a key's relationship to a
                          set of values. Valid operators are In, NotIn, Exists and
                          DoesNotExist.
                        type: string
                      values:
                        description: values is an array of string values. If the operator
                          is In or NotIn, the values array must be non-empty. If the
                          operator is Exists or DoesNotExist, the values array must
                          be empty. This array is replaced during a strategic merge
                          patch.
                        items:
                          type: string
                        type: array
                    required:
                    - key
                    - operator
                    type: object
                  type: array
                matchLabels:
                  additionalProperties:
                    type: string
                  description: matchLabels is a map of {key,value} pairs. A single
                    {key,value} in the matchLabels map is equivalent to an element
                    of matchExpressions, whose key field is "key", the operator is
                    "In", and the values array contains only "value". The requirements
                    are ANDed.
                  type: object
              type: object
            controller:
              description: Controller is the AVI Controller endpoint to which AKO
                talks to provision Load Balancer resources The format is [scheme://]address[:port]
                * scheme                     http or https, defaults to https if not                              specified
                * address                    IP address of the AVI Controller                              specified
                * port                       if not specified, use default port for                              the
                corresponding scheme
              type: string
            dataNetwork:
              description: DataNetworks describes the Data Networks the AKO will be
                deployed with. This field is immutable.
              properties:
                cidr:
                  type: string
                ipPools:
                  items:
                    description: IPPool defines a contiguous range of IP Addresses
                    properties:
                      end:
                        description: End represents the ending IP address of the pool.
                        type: string
                      start:
                        description: Start represents the starting IP address of the
                          pool.
                        type: string
                      type:
                        description: Type represents the type of IP Address
                        enum:
                        - V4
                        type: string
                    required:
                    - end
                    - start
                    - type
                    type: object
                  type: array
                name:
                  type: string
              required:
              - cidr
              - ipPools
              - name
              type: object
            extraAviConfig:
              description: ExtraConfig contains the extra configurations for AKO to
                override defaults
              type: object
            tenant:
              description: The AVI tenant for the current AKODeploymentConfig This
                field is optional.
              properties:
                context:
                  description: Context is the type of AVI tenant context. Defaults
                    to Provider. This field is immutable.
                  enum:
                  - Provider
                  - Tenant
                  type: string
                name:
                  description: Name is the name of the tenant. This field is immutable.
                  type: string
              required:
              - name
              type: object
            workloadCredentialRef:
              description: "WorkloadCredentialRef points to a Secret resource which
                includes the username and password to access and configure the Avi
                Controller. \n * username                   Username used with basic
                authentication for                              the Avi REST API *
                password                   Password used with basic authentication
                for                              the Avi REST API \n This field is
                optional. When it's not specified, username/password will be automatically
                generated for each Cluster and Tenant needs to be non-nil in this
                case."
              properties:
                name:
                  description: Name is the name of resource being referenced.
                  type: string
                namespace:
                  description: Namespace of the resource being referenced.
                  type: string
              required:
              - name
              - namespace
              type: object
          required:
          - certificateAuthorityRef
          - cloudName
          - controller
          - dataNetwork
          type: object
        status:
          description: AKODeploymentConfigStatus defines the observed state of AKODeploymentConfig
          properties:
            conditions:
              description: Conditions defines current state of the AKODeploymentConfig.
              items:
                description: Condition defines an observation of a Cluster API resource
                  operational state.
                properties:
                  lastTransitionTime:
                    description: Last time the condition transitioned from one status
                      to another. This should be when the underlying condition changed.
                      If that is not known, then using the time when the API field
                      changed is acceptable.
                    format: date-time
                    type: string
                  message:
                    description: A human readable message indicating details about
                      the transition. This field may be empty.
                    type: string
                  reason:
                    description: The reason for the condition's last transition in
                      CamelCase. The specific API may choose whether or not this field
                      is considered a guaranteed API. This field may not be empty.
                    type: string
                  severity:
                    description: Severity provides an explicit classification of Reason
                      code, so the users or machines can immediately understand the
                      current situation and act accordingly. The Severity field MUST
                      be set only when Status=False.
                    type: string
                  status:
                    description: Status of the condition, one of True, False, Unknown.
                    type: string
                  type:
                    description: Type of condition in CamelCase or in foo.example.com/CamelCase.
                      Many .condition.type values are consistent across resources
                      like Available, but because arbitrary conditions can be useful
                      (see .node.status.conditions), the ability to deconflict is
                      important.
                    type: string
                required:
                - status
                - type
                type: object
              type: array
            observedGeneration:
              description: ObservedGeneration reflects the generation of the most
                recently observed AKODeploymentConfig.
              format: int64
              type: integer
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
